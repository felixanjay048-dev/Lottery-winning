import tkinter as tk

from tkinter import Label, Entry

root = tk.Tk()

root.geometry("400x500")

label = tk.Label(root, text="Username", font="Arial")

entry = tk.Entry(root)

entry.place(x=135, y=80)

label.place(x=150, y=50)

label2 = tk.Label(root, text="Password", font="Arial")

label2.place(x=150, y=100)

entry2 = tk.Entry(root)

entry2.place(x=135, y=130)

label3 = tk.Label(root, text="Re-write your username", font="Arial")

label3.place(x=90, y=150)

entry3 = Entry(root)

entry3.place(x=135, y=180)

label4 = tk.Label(root, text="Re-write your password", font="Arial")

label4.place(x= 90, y=200)

entry4 = tk.Entry(root)

entry4.place(x=135, y=230) 

root.mainloop()
